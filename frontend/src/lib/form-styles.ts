/**
 * Clases compartidas para inputs/textareas/selects "de formulario completo"
 * (fondo blanco, borde slate, anillo de foco). Antes se redefinían por
 * separado en AdminProductPage y AgendarPage con el mismo valor.
 */
export const formInputClass =
  'w-full rounded-lg border border-slate-300 px-4 py-2.5 transition-all focus:border-transparent focus:ring-2 focus:ring-slate-900/20';

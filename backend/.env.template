# Copia este archivo como .env y rellena los valores. NO subas .env a git.

STAGE=dev
PORT=3001

# --- Base de datos ---
# Local (Docker): usa estos valores.
# Supabase (prod): pon aquí el host/usuario/password del proyecto y STAGE=prod.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=LuneByDB
DB_USERNAME=postgres
DB_PASSWORD=cambia_esto_en_local

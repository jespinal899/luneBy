import { Module } from '@nestjs/common';

/**
 * Utilidades transversales (DTOs de paginación, pipes, helpers).
 * De momento solo agrupa el PaginationDto que se comparte entre módulos.
 */
@Module({})
export class CommonModule {}
